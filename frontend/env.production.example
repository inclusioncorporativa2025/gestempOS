# Copiar a .env.production en el servidor antes de `npm run build` (opcional).
# Si no existe, vite.config.js usa /api/ y los dominios de producción por defecto.

VITE_API_BASE_URL=/api/
VITE_APP_URL=https://app.fichaeneltrabajo.es
VITE_LANDING_URL=https://fichaeneltrabajo.es
